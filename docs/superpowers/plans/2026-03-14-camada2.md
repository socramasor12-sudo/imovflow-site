# Camada 2 — Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar og:image/twitter:image para SEO de compartilhamento, CPT `imovel` com meta box e cards dinâmicos na home, e overrides CSS mobile para 375px.

**Architecture:** Tudo em `functions.php` (seguindo padrão do CPT `captacao` existente), `front-page.php` (substituindo cards estáticos por WP_Query), e `assets/css/main.css` (adicionando regras no bloco `@media (max-width: 768px)` existente). Sem novos arquivos.

**Tech Stack:** WordPress tema customizado, PHP 8+, CSS3 (sem build step), nenhuma dependência nova.

**Spec:** `docs/superpowers/specs/2026-03-14-camada2-design.md`

---

## Chunk 1: SEO — og:image e twitter:image

### Task 1: Adicionar og:image e twitter:image em `functions.php`

**Files:**
- Modify: `functions.php:136` (após linha `og:url`, dentro de `marcos_rosa_seo_meta()`)

Contexto — estado atual do bloco OG (linha 130–140):
```php
<!-- Open Graph -->
<meta property="og:type"        content="website">
<meta property="og:locale"      content="pt_BR">
<meta property="og:site_name"   content="Marcos Rosa — Corretor Imobiliário">
<meta property="og:title"       content="...">
<meta property="og:description" content="...">
<meta property="og:url"         content="...">     ← inserir APÓS esta linha
<!-- Twitter Card -->
<meta name="twitter:card"        content="summary_large_image">
<meta name="twitter:title"       content="...">
<meta name="twitter:description" content="...">
```

- [ ] **Step 1: Adicionar as 4 linhas de imagem após `og:url`**

Em `functions.php`, após a linha com `og:url` (linha 136) e antes do comentário `<!-- Twitter Card -->`, inserir:

```php
<meta property="og:image"        content="<?php echo esc_url(get_template_directory_uri()); ?>/assets/img/logo.png">
<meta property="og:image:width"  content="512">
<meta property="og:image:height" content="512">
<meta name="twitter:image"       content="<?php echo esc_url(get_template_directory_uri()); ?>/assets/img/logo.png">
```

- [ ] **Step 2: Verificar no browser**

No servidor, abrir imovflow.com.br e inspecionar o `<head>`:
```
Ctrl+U (view source) → buscar "og:image"
```
Esperado: 3 linhas `og:image*` e 1 linha `twitter:image` presentes.

Alternativa rápida via curl:
```bash
curl -s https://imovflow.com.br | grep "og:image\|twitter:image"
```
Esperado: 4 linhas de output.

- [ ] **Step 3: Commit**

```bash
git add functions.php
git commit -m "feat: adicionar og:image e twitter:image para compartilhamento social"
```

---

## Chunk 2: CPT Imóveis — Registro, Meta Box e WP_Query

### Task 2: Registrar CPT `imovel` em `functions.php`

**Files:**
- Modify: `functions.php` (após linha 103 — `add_action('init', 'marcos_rosa_cpt')`)

- [ ] **Step 1: Adicionar função CPT após o bloco `captacao`**

Em `functions.php`, após a linha `add_action('init', 'marcos_rosa_cpt');` (linha 103), inserir bloco completo:

```php

// ─── CPT: IMOVEL ────────────────────────────────────────────
function marcos_rosa_cpt_imovel() {
    register_post_type('imovel', [
        'label'           => 'Imóveis',
        'public'          => true,
        'show_ui'         => true,
        'show_in_menu'    => true,
        'menu_icon'       => 'dashicons-admin-home',
        'rewrite'         => ['slug' => 'imoveis', 'with_front' => false],
        'has_archive'     => true,
        'supports'        => ['title', 'thumbnail'],
        'capability_type' => 'post',
    ]);
}
add_action('init', 'marcos_rosa_cpt_imovel');
```

- [ ] **Step 2: Verificar no WP Admin**

Fazer deploy e acessar `/wp-admin/`. Verificar:
- Menu lateral tem item "Imóveis" com ícone de casa (`dashicons-admin-home`)
- Clicar em "Imóveis" → página de listagem carrega sem erros PHP

- [ ] **Step 3: Commit**

```bash
git add functions.php
git commit -m "feat: registrar CPT imovel"
```

---

### Task 3: Adicionar meta box com campos do imóvel

**Files:**
- Modify: `functions.php` (após o bloco CPT `imovel` adicionado na Task 2)

- [ ] **Step 1: Adicionar função de registro da meta box**

Em `functions.php`, após `add_action('init', 'marcos_rosa_cpt_imovel');`, inserir:

```php

// ─── META BOX: IMOVEL ───────────────────────────────────────
function marcos_rosa_imovel_meta_box_register() {
    add_meta_box(
        'marcos_rosa_imovel_detalhes',
        'Detalhes do Imóvel',
        'marcos_rosa_imovel_meta_box',
        'imovel',
        'normal',
        'high'
    );
}
add_action('add_meta_boxes', 'marcos_rosa_imovel_meta_box_register');

function marcos_rosa_imovel_meta_box($post) {
    wp_nonce_field('marcos_rosa_save_imovel', 'marcos_rosa_imovel_nonce');
    $tipo      = get_post_meta($post->ID, '_imovel_tipo',      true);
    $bairro    = get_post_meta($post->ID, '_imovel_bairro',    true);
    $preco     = get_post_meta($post->ID, '_imovel_preco',     true);
    $quartos   = get_post_meta($post->ID, '_imovel_quartos',   true);
    $banheiros = get_post_meta($post->ID, '_imovel_banheiros', true);
    $vagas     = get_post_meta($post->ID, '_imovel_vagas',     true);
    $badge     = get_post_meta($post->ID, '_imovel_badge',     true);
    ?>
    <table class="form-table">
      <tr>
        <th><label for="imovel_tipo">Tipo</label></th>
        <td>
          <select id="imovel_tipo" name="imovel_tipo">
            <option value="">— selecione —</option>
            <?php foreach (['Apartamento','Casa','Terreno','Comercial'] as $opt): ?>
              <option value="<?php echo esc_attr($opt); ?>" <?php selected($tipo, $opt); ?>><?php echo esc_html($opt); ?></option>
            <?php endforeach; ?>
          </select>
        </td>
      </tr>
      <tr>
        <th><label for="imovel_bairro">Bairro</label></th>
        <td><input type="text" id="imovel_bairro" name="imovel_bairro" value="<?php echo esc_attr($bairro); ?>" placeholder="ex: Jundiaí" class="regular-text"></td>
      </tr>
      <tr>
        <th><label for="imovel_preco">Preço (R$)</label></th>
        <td><input type="text" id="imovel_preco" name="imovel_preco" value="<?php echo esc_attr($preco); ?>" placeholder="ex: 350.000" class="regular-text"></td>
      </tr>
      <tr>
        <th><label for="imovel_quartos">Quartos</label></th>
        <td><input type="number" id="imovel_quartos" name="imovel_quartos" value="<?php echo esc_attr($quartos); ?>" min="0" class="small-text"></td>
      </tr>
      <tr>
        <th><label for="imovel_banheiros">Banheiros</label></th>
        <td><input type="number" id="imovel_banheiros" name="imovel_banheiros" value="<?php echo esc_attr($banheiros); ?>" min="0" class="small-text"></td>
      </tr>
      <tr>
        <th><label for="imovel_vagas">Vagas</label></th>
        <td><input type="number" id="imovel_vagas" name="imovel_vagas" value="<?php echo esc_attr($vagas); ?>" min="0" class="small-text"></td>
      </tr>
      <tr>
        <th><label for="imovel_badge">Badge</label></th>
        <td><input type="text" id="imovel_badge" name="imovel_badge" value="<?php echo esc_attr($badge); ?>" placeholder="ex: LANÇAMENTO" class="regular-text"></td>
      </tr>
    </table>
    <?php
}
```

- [ ] **Step 2: Adicionar save handler**

Imediatamente após a função `marcos_rosa_imovel_meta_box`, inserir:

```php

function marcos_rosa_save_imovel_meta($post_id) {
    if ( defined('DOING_AUTOSAVE') && DOING_AUTOSAVE )           return $post_id;
    if ( ! isset($_POST['marcos_rosa_imovel_nonce']) )           return $post_id;
    if ( ! wp_verify_nonce($_POST['marcos_rosa_imovel_nonce'],
                           'marcos_rosa_save_imovel') )          return $post_id;
    if ( ! current_user_can('edit_post', $post_id) )             return $post_id;

    $campos_texto = ['imovel_tipo','imovel_bairro','imovel_preco','imovel_badge'];
    foreach ($campos_texto as $campo) {
        if (isset($_POST[$campo])) {
            update_post_meta($post_id, '_' . $campo, sanitize_text_field($_POST[$campo]));
        }
    }
    $campos_num = ['imovel_quartos','imovel_banheiros','imovel_vagas'];
    foreach ($campos_num as $campo) {
        if (isset($_POST[$campo])) {
            update_post_meta($post_id, '_' . $campo, absint($_POST[$campo]));
        }
    }
    return $post_id;
}
add_action('save_post_imovel', 'marcos_rosa_save_imovel_meta');
```

- [ ] **Step 3: Verificar no WP Admin**

Fazer deploy. Em `/wp-admin/post-new.php?post_type=imovel`:
- Meta box "Detalhes do Imóvel" aparece abaixo do editor de título
- Preencher: Tipo=Apartamento, Bairro=Jundiaí, Preço=350.000, Quartos=3, Banheiros=2, Vagas=2, Badge=LANÇAMENTO
- Publicar → reeditar o post → campos mantêm os valores salvos
- Sem erros PHP no WP_DEBUG

- [ ] **Step 4: Commit**

```bash
git add functions.php
git commit -m "feat: meta box e save handler para CPT imovel"
```

---

### Task 4: Substituir cards estáticos por WP_Query em `front-page.php`

**Files:**
- Modify: `front-page.php:70-96` (seção `<!-- LANÇAMENTOS -->` completa)

Estado atual das linhas 70–96 (a ser substituído integralmente):
```html
<!-- LANÇAMENTOS -->
<section class="imoveis-section" id="imoveis">
  <div class="section-header">
    <div>
      <div class="section-tag">Novidades</div>
      <h2 class="section-titulo">Lançamentos em<br><span>Anápolis/GO</span></h2>
    </div>
    <a href="#" class="ver-todos">Ver todos</a>
  </div>
  <div class="imoveis-grid">
    <div class="imovel-card"> ... card estático 1 ... </div>
    <div class="imovel-card"> ... card estático 2 ... </div>
    <div class="imovel-card"> ... card estático 3 ... </div>
  </div>
</section>
```

- [ ] **Step 1: Substituir o bloco inteiro pela versão dinâmica**

Substituir linhas 70–96 de `front-page.php` por:

```php
<!-- LANÇAMENTOS -->
<?php
$imoveis = new WP_Query([
    'post_type'      => 'imovel',
    'posts_per_page' => 3,
    'post_status'    => 'publish',
]);
if ($imoveis->have_posts()) : ?>
<section class="imoveis-section" id="imoveis">
  <div class="section-header">
    <div>
      <div class="section-tag">Novidades</div>
      <h2 class="section-titulo">Lançamentos em<br><span>Anápolis/GO</span></h2>
    </div>
    <a href="<?php echo esc_url(get_post_type_archive_link('imovel')); ?>" class="ver-todos">Ver todos</a>
  </div>
  <div class="imoveis-grid">
    <?php while ($imoveis->have_posts()) : $imoveis->the_post();
      $tipo      = get_post_meta(get_the_ID(), '_imovel_tipo',      true);
      $bairro    = get_post_meta(get_the_ID(), '_imovel_bairro',    true);
      $preco     = get_post_meta(get_the_ID(), '_imovel_preco',     true);
      $quartos   = get_post_meta(get_the_ID(), '_imovel_quartos',   true);
      $banheiros = get_post_meta(get_the_ID(), '_imovel_banheiros', true);
      $vagas     = get_post_meta(get_the_ID(), '_imovel_vagas',     true);
      $badge     = get_post_meta(get_the_ID(), '_imovel_badge',     true);
    ?>
    <?php // Nota: estrutura 3-blocos (imovel-info + imovel-rodape) intencional — corresponde ao CSS existente. O spec usa estrutura diferente; ignorar o spec neste ponto. ?>
    <div class="imovel-card">
      <div class="imovel-img">
        <?php if (has_post_thumbnail()) :
          the_post_thumbnail('medium', ['class' => 'imovel-thumb']);
        else : ?>
          <div class="imovel-img-placeholder">MR</div>
        <?php endif; ?>
        <?php if ($badge) : ?>
          <div class="imovel-badge"><?php echo esc_html($badge); ?></div>
        <?php endif; ?>
      </div>
      <div class="imovel-info">
        <div class="imovel-tipo"><?php echo esc_html($tipo); ?></div>
        <div class="imovel-nome"><?php the_title(); ?></div>
        <?php if ($bairro) : ?>
          <div class="imovel-local">📍 <?php echo esc_html($bairro); ?>, Anápolis/GO</div>
        <?php endif; ?>
      </div>
      <div class="imovel-rodape">
        <?php if ($preco) : ?>
          <div class="imovel-preco">R$ <?php echo esc_html($preco); ?> <span>a partir de</span></div>
        <?php endif; ?>
        <?php if ($quartos || $banheiros || $vagas) : ?>
        <div class="imovel-detalhes">
          <?php if ($quartos)   echo '<div class="detalhe"><div class="detalhe-num">' . absint($quartos)   . '</div><div class="detalhe-label">Qtos</div></div>'; ?>
          <?php if ($banheiros) echo '<div class="detalhe"><div class="detalhe-num">' . absint($banheiros) . '</div><div class="detalhe-label">Banh</div></div>'; ?>
          <?php if ($vagas)     echo '<div class="detalhe"><div class="detalhe-num">' . absint($vagas)     . '</div><div class="detalhe-label">Vagas</div></div>'; ?>
        </div>
        <?php endif; ?>
      </div>
    </div>
    <?php endwhile; wp_reset_postdata(); ?>
  </div>
</section>
<?php endif; ?>
```

- [ ] **Step 2: Verificar com imóvel publicado**

Com o imóvel de teste da Task 3 ainda publicado, acessar imovflow.com.br:
- Seção "Lançamentos" exibe 1 card com os dados reais (Apartamento · Jundiaí · R$ 350.000 · 3/2/2)
- Badge "LANÇAMENTO" aparece sobreposto à imagem/placeholder

- [ ] **Step 3: Verificar estado vazio**

No WP Admin, despublicar o imóvel de teste (mudar status para Rascunho). Recarregar home:
- Seção "Lançamentos" desaparece completamente (não exibe container vazio)

- [ ] **Step 4: Republicar o imóvel de teste**

Devolver ao status "Publicado" para a próxima fase.

- [ ] **Step 5: Commit**

```bash
git add front-page.php
git commit -m "feat: cards de imóveis dinâmicos via WP_Query (CPT imovel)"
```

---

## Chunk 3: Responsividade Mobile

### Task 5: Adicionar base desktop `.mr-footer-grid` e overrides mobile em `main.css`

**Files:**
- Modify: `assets/css/main.css`
  - Linha ~256: adicionar `.mr-footer-grid` base desktop (ao lado de `.footer-grid`)
  - Linha ~472: adicionar overrides dentro do bloco `@media (max-width: 768px)` existente

**Por que duas edições?** O `.footer-grid` (sem prefixo) na linha 256 é CSS morto — o footer usa `mr-footer-grid`. A regra desktop precisa ser adicionada para que o footer tenha layout em 4 colunas antes do breakpoint.

- [ ] **Step 1: Adicionar regra desktop `.mr-footer-grid` após `.footer-grid`**

Em `main.css`, após a linha que contém `.footer-grid { display: grid; grid-template-columns: 2fr 1fr 1fr 1fr; ... }` (linha ~256), inserir:

```css
  .mr-footer-grid { display: grid; grid-template-columns: 2fr 1fr 1fr 1fr; gap: 60px; margin-bottom: 60px; }
```

- [ ] **Step 2: Adicionar overrides mobile dentro do bloco `@media (max-width: 768px)` existente**

O bloco `@media (max-width: 768px)` termina na linha ~472 com `}`. Inserir os overrides **antes** do `}` de fechamento do media query:

```css
    /* ─── MOBILE — SEÇÕES ─── */
    .hero-esquerda {
      padding: 100px 24px 60px;
    }
    .hero-direita {
      height: 260px;
    }
    .hero-stats {
      gap: 16px;
      padding: 24px;
    }
    .imoveis-grid {
      grid-template-columns: 1fr;
    }
    /* busca-section está display:none mas overrides preparados para ativação futura */
    .busca-section {
      padding: 40px 24px;
    }
    .busca-form {
      grid-template-columns: 1fr;
    }
    .btn-busca {
      width: 100%;
    }
    .sobre-section {
      grid-template-columns: 1fr;
      padding: 60px 24px;
    }
    .sobre-foto {
      min-height: 280px;
    }
    .captacao-section {
      grid-template-columns: 1fr;
      padding: 60px 24px;
    }
    .cta-section {
      flex-direction: column;
      padding: 60px 24px;
      gap: 32px;
      text-align: center;
    }
    .mr-footer {
      padding: 60px 24px 40px;
    }
    .mr-footer-grid {
      grid-template-columns: 1fr;
      gap: 40px;
    }
    .mr-footer-bottom {
      flex-direction: column;
      gap: 16px;
      text-align: center;
    }
```

- [ ] **Step 3: Verificar com DevTools a 375px**

Abrir imovflow.com.br no Chrome → DevTools (F12) → Toggle Device Toolbar → selecionar "iPhone SE" (375×667):

Verificar cada seção visualmente e com o console:
```js
document.documentElement.scrollWidth > window.innerWidth  // deve retornar false
```

Nota: `document.body.scrollWidth` é unreliável quando `body { overflow-x: hidden }` está ativo (retorna false positivo). Usar `documentElement` para detectar overflow real.

Checklist visual:
- [ ] Hero: coluna única, sem corte lateral
- [ ] Cards de imóveis: 1 por linha
- [ ] Seção Sobre: empilhada verticalmente
- [ ] Captação: formulário em coluna única
- [ ] CTA: botão abaixo do texto
- [ ] Footer: colunas empilhadas, sem overflow

- [ ] **Step 4: Commit**

```bash
git add assets/css/main.css
git commit -m "feat: responsividade mobile 375px — overrides e base mr-footer-grid"
```

---

## Deploy Final

- [ ] **Step 1: Push para GitHub**

```bash
git push origin master
```

- [ ] **Step 2: Deploy no servidor**

```bash
ssh imovflow@191.6.209.223 'git -C ~/www/wp-content/themes/marcos-rosa pull origin master && cd ~/www && wp cache flush --allow-root'
```

- [ ] **Step 3: Smoke test em produção**

```bash
curl -s https://imovflow.com.br | grep "og:image"          # deve retornar 3+ linhas
curl -s https://imovflow.com.br | grep "twitter:image"     # deve retornar 1 linha
```

Acessar `/wp-admin/edit.php?post_type=imovel` — menu presente, sem erros.
